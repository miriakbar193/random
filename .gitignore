# Secrets — never commit. The LLM key is injected from Vault in the deployed app.
.env
.env.*
!.env.example
*.key
*.pem
# Local run config (holds your Cursor API key for run-local.*)
local-config.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Version snapshots kept by the archive-before-editing convention
0. Archive/

# OneDrive / Windows cruft
desktop.ini
Thumbs.db
.DS_Store

# Node (only if a build step is added later)
node_modules/
